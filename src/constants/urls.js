const host = "";
const urls = {
  banks: host + "/banks",
  transactions: host + "/transactions",
  delete: host + "/delete",
  add: host + "/transactions",
  login: host + "/login",
  logout: host + "/logout"
};
export default urls;
